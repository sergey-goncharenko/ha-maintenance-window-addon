# Screenshots

Drop public-facing screenshots for the repository README in this folder.

Expected files:

- `config-nightly-maintenance.png` — the main nightly Core maintenance window with selected days.
- `config-emergency-ssh-daily.png` — the emergency SSH window with no selected days, showing the daily default.

Keep screenshots focused on the add-on configuration UI. Avoid including hostnames, tokens, private URLs, device names, or other sensitive Home Assistant data.
